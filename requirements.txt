# Python tooling for the #90DaysOfDataEngineering workshop / applied examples.
#
#   python3 -m venv .venv
#   source .venv/bin/activate      # or use .venv/bin/python directly
#   pip install -r requirements.txt
#
# Used by: the dlt workshop (examples/workshop-dlt), the cricket applied example
# (examples/cricket), and the Trino client for the Module 3 lakehouse.

dlt[postgres,pyiceberg]>=1.28   # ingestion: Postgres + Iceberg (pyiceberg) destinations
openpyxl>=3.1                   # read the bowling/fielding .xlsx exports
sqlalchemy>=2.0.18              # required by dlt's pyiceberg/rest-catalog path
s3fs>=2024.0.0                  # pyiceberg FsspecFileIO -> S3/MinIO
trino>=0.330                    # Trino client (query the lakehouse)
psycopg2-binary>=2.9            # Postgres driver for quick checks
dbt-trino>=1.8                  # Module 4: dbt transformations on the Trino/Iceberg lakehouse
duckdb>=1.4                     # Day 38: local engine — reads Iceberg (iceberg_scan) + Postgres
dbt-duckdb>=1.9                 # Day 47: run the dbt project locally on DuckDB (fast dev)
soda-core-postgres>=3.5         # Module 5: Soda Core data-quality scans on the Postgres landing zone
pandera[pandas]>=0.26           # Module 5: DataFrame schema validation (in-flight, at ingest)
